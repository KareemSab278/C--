#include <stdlib.h>
#include <stdio.h>
#include "qsort.h"

void intSet(int input[])
{
    int output[] = {};
    int inputLen = arrSizeOf(input);

    for (int i = 0; i < inputLen; i++)
    {
           
    }
}